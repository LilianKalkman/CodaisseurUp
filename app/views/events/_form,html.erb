<!-- <h1> Super lelijke form, doei </h1> -->

<%= form_for @room, html: { multipart: true } do |f| %>

<label>Photos</label>

<div class="form-group">
  <span class="btn btn-default">
    Upload Photos
    <%= file_field_tag "images[]", type: :file, multiple: true %>
  </span>
</div>
